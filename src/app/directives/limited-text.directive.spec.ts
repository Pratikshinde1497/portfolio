import { LimitedTextDirective } from './limited-text.directive';

describe('LimitedTextDirective', () => {
  it('should create an instance', () => {
    const directive = new LimitedTextDirective();
    expect(directive).toBeTruthy();
  });
});
