import { StyleImageDirective } from './style-image.directive';

describe('StyleImageDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleImageDirective();
    expect(directive).toBeTruthy();
  });
});
